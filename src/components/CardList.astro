---
import Card from "./Card.astro";
import { t } from "i18next";

// La función t de i18next  se usa para obtener las traducciones para la clave "cards".
// En este caso, La función t toma dos argumentos:
// 1- La clave de traducción: En este caso "cards". Esta clave corresponde a las entradas de traducción pasados en el init de a i18next.
// 2- Las opciones: En este caso { returnObjects: true }, que le dice a i18next que devuelva objetos o arrays en lugar de solo cadenas. Esto es útil cuando hay estructuras de datos más complejas en los archivos dentro de locales para traducción.
// Por lo tanto, t("cards", { returnObjects: true }) está obteniendo el valor asociado con la clave "cards" en los recursos de traducción y devolviéndolo como un objeto o un array en la variable cards.

let cards = t("cards", { returnObjects: true });
---

<section
  class="w-11/12 p-4 grid grid-rows-2 grid-cols-4 gap-4"
>
  {
    cards.map((card) => (
      <Card
        id={card.id}
        name={card.name}
        status={card.status}
        title={card.title}
        txt={card.txt}
        img={card.img}
        color={card.color}
      />
    ))
  }
</section>
